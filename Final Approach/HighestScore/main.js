$('#scratch').wScratchPad({
    size: 100, // The size of the brush/scratch.
    /*bg: '001.jpg', */ // Background (image path or hex color).
    fg: '#000' // Foreground (image path or hex color).
});